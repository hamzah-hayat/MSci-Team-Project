<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent" android:layout_height="fill_parent"
    android:id="@+id/choose_balls_puzzle"
    android:paddingTop="15dp">

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/ball_difficulty_dd"
        android:layout_above="@+id/playGame"
        android:layout_marginBottom="51dp"
        android:layout_alignRight="@+id/playGame"
        android:layout_alignEnd="@+id/playGame"
        android:layout_alignLeft="@+id/playGame"
        android:layout_alignStart="@+id/playGame" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/playGame"
        android:src="@drawable/ic_play"
        android:background="#ffffffff"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/random"
        android:layout_alignStart="@+id/random" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/random"
        android:src="@drawable/ic_random"
        android:background="#ffffffff"
        android:layout_below="@+id/playGame"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="51dp" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/choose_puzzle"
        android:src="@drawable/choose_puzzle_header"
        android:background="#ffffffff"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />
</RelativeLayout>